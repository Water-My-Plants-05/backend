const router = require('express').Router()

const Plants = require('./plants-model')

// router.get('/', (req,res,next){
//     Plants.getAllPlants()
//         .then(plants => {
//             res.json(p)
//         })
// })
